#!/bin/bash
#file="Довольно распространённая ошибка ошибка— это лишний повтор повтор слова слова. Смешно, не не правда ли? Не нужно портить хор хоровод."
#echo $file | awk '{
#    for (i=1; i<=NF; i++) {
#        if (!($i in a)) {
#            a[$i];
#            printf "%s%s", $i, (i==NF ? RS : FS)
#        }
#    }
#}'
# sed -r 's/\b(\w+)(\s+\1)+\b/\1/gi'

# Проверяем, был ли предоставлен аргумент
if [ "$#" -ne 1 ]; then
    echo "Пожалуйста, предоставьте имя файла как аргумент."
    exit 1
fi

# Читаем текст из файла и удаляем повторяющиеся слова
sed -r 's/\b(\w+)(\s+\1)+\b/\1/gi' "$1"
